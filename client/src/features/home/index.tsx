import { AccountBalance } from "../accountBalance";

export const Home = () => {
    return (
        <div className="container mx-auto p-2 sm:p-4 md:p-6">
            <AccountBalance />
        </div>
    );
};
