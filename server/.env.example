# ===========================================
# DEPRECATED: Use docker/.env instead
# ===========================================
# For local development, create a symlink to docker/.env:
# 
# Windows (PowerShell as Admin):
#   New-Item -ItemType SymbolicLink -Path ".env" -Target "..\docker\.env"
#
# Linux/Mac:
#   ln -s ../docker/.env .env
#
# Or simply copy docker/.env.example to docker/.env and use that
# ===========================================

# This file is kept for reference only.
# See docker/.env.example for all available configuration options.
